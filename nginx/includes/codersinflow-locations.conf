server {
    # Redirect HTTP to HTTPS
    listen 80;
    listen [::]:80;
    server_name codersinflow.com www.codersinflow.com;
    return 301 https://$host$request_uri;
}

server {
    # HTTPS configuration
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    include snippets/block-middleware-subrequest.conf;

    # Define the server names
    server_name codersinflow.com www.codersinflow.com;

    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/codersinflow.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/codersinflow.com/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/codersinflow.com/chain.pem;

    # Recommended SSL settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    # HSTS (uncomment if you're sure)
    # add_header Strict-Transport-Security "max-age=63072000" always;

    # Set the root directory for the website
    root /var/www/codersinflow.com;

    # Define the index files
    index index.html index.htm;

    # Add logs for debugging and access tracking
    access_log /var/log/nginx/codersinflow.com.access.log;
    error_log /var/log/nginx/codersinflow.com.error.log;

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;

    # Redirect all PHP files to /var/www/php/filename.php
    location ~ \.php$ {
        # Extract the filename from the request
        set $php_filename $uri;

        # Pass the request to PHP-FPM
        root /var/www/codersinflow.com;
        fastcgi_pass unix:/var/run/php/php-fpm.sock; # Adjust to your PHP-FPM configuration
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    # Include Codersinflow unified server location blocks
    include /etc/nginx/includes/codersinflow-locations.conf;

    # API endpoint for version detection (static files)
    location /api/ {
        try_files $uri $uri/ =404;
    }

    # Downloads directory - ensure it's accessible
    location /downloads/ {
        autoindex off;  # Set to 'on' if you want directory listing
        try_files $uri $uri/ =404;
        
        # Add headers for download files
        add_header Content-Disposition "attachment";
    }

    # Main location block for Astro/static files
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|vsix)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        
        # Special handling for .vsix files
        location ~* \.vsix$ {
            add_header Content-Type "application/vsix";
            add_header Content-Disposition "attachment";
        }
    }

    # Redirect www to non-www (optional)
    if ($host = www.codersinflow.com) {
        return 301 https://codersinflow.com$request_uri;
    }
}
cat: more: No such file or directory
# Admin interface location block
# Exact match for /admin (add trailing slash)
location = /admin {
    return 301 /admin/;
}

# Remove trailing slashes from admin subpaths
location ~ ^/admin/(.+)/$ {
    add_header Cache-Control "no-cache, no-store, must-revalidate";
    add_header Pragma "no-cache";
    add_header Expires "0";
    return 301 /admin/$1;
}

# Main admin location block
location /admin/ {
    # Proxy to admin UI server (Astro SSR)
    proxy_pass http://codersinflow_admin/admin/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port $server_port;
    
    # Timeout settings
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    
    # Buffer settings
    proxy_buffering on;
    proxy_buffer_size 4k;
    proxy_buffers 8 4k;
    proxy_busy_buffers_size 8k;
    
    # Disable caching for admin pages (including Cloudflare)
    proxy_cache off;
    add_header Cache-Control "no-cache, no-store, must-revalidate, private, max-age=0";
    add_header Pragma "no-cache";
    add_header Expires "0";
    add_header X-Accel-Expires "0";
    add_header Surrogate-Control "no-store";
}

# Admin UI assets - no caching for JS/CSS (including Cloudflare)
location ~* ^/admin/.*\.(js|css)$ {
    proxy_pass http://codersinflow_admin;
    proxy_cache off;
    expires -1;
    add_header Cache-Control "no-cache, no-store, must-revalidate, private, max-age=0";
    add_header Pragma "no-cache";
    add_header X-Accel-Expires "0";
    add_header Surrogate-Control "no-store";
    
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# Cache other static assets in admin UI (images, fonts)
location ~* ^/admin/.*\.(png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
    proxy_pass http://codersinflow_admin;
    proxy_cache_valid 200 1h;
    expires 1h;
    add_header Cache-Control "public";
    
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# API proxy endpoints
location /v1/ {
    proxy_pass http://codersinflow_server;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Special handling for streaming responses
    proxy_buffering off;
    proxy_cache off;
    
    # Timeout settings for AI requests
    proxy_connect_timeout 60s;
    proxy_send_timeout 300s;  # 5 minutes for long AI responses
    proxy_read_timeout 300s;  # 5 minutes for streaming
    
    # Handle large request bodies
    client_max_body_size 10M;
}

# WebSocket endpoint
location /ws {
    proxy_pass http://codersinflow_server;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # WebSocket timeout settings
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 300s;  # 5 minutes for long connections
    
    # Disable buffering for real-time communication
    proxy_buffering off;
}

# Admin API endpoints
location /admin-api/ {
    proxy_pass http://codersinflow_server;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Timeout settings for admin operations
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    
    # Handle request bodies for creating/updating data
    client_max_body_size 1M;
}

# Registration endpoint (email API key)
location /register {
    proxy_pass http://codersinflow_server;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Settings for registration requests
    proxy_connect_timeout 30s;
    proxy_send_timeout 30s;
    proxy_read_timeout 30s;
    
    # Handle request bodies for registration data
    client_max_body_size 1M;
}

# Health check endpoint
location /health {
    proxy_pass http://codersinflow_server;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Quick health check settings
    proxy_connect_timeout 5s;
    proxy_send_timeout 5s;
    proxy_read_timeout 5s;
}


# API endpoints for v1
location /api/v1/ {
    proxy_pass http://codersinflow_server;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Settings for API requests
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;
    
    # Handle request bodies
    client_max_body_size 10M;