# MongoDB connection
MONGODB_URI=mongodb://admin:${MONGO_PASSWORD}@localhost:${MONGODB_PORT}/${DB_NAME}?authSource=admin

# Backend server port
PORT=${BACKEND_PORT}

# JWT secret for authentication
JWT_SECRET=${JWT_SECRET}

# CORS origin for frontend
CORS_ORIGIN=https://${DOMAIN}

# MongoDB password (should match docker-compose)
MONGO_PASSWORD=${MONGO_PASSWORD}